Implement src/runner.rs with run_snobol4(src, data, max_instrs) -> Result<RunResult,String>. Construct a fresh cor24_emulator::EmulatorCore, load SNOBOL4_BIN at 0x0, src bytes at 0x080000, optional data at 0x090000, set entry/PC=0, run with the given instruction budget, drain UART TX into a String. Mirror what scripts/run-snobol4.sh in sw-cor24-snobol4 does. If the emulator crate lacks a load_bytes API, look at how web-sw-cor24-macrolisp's repl.rs loads its program and follow the same pattern.