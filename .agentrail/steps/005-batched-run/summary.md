Refactored runner into Session::new+tick API. App owns the Session and drives execution via gloo Timeout in 200k batches with live status updates (instructions, elapsed ms). Run/Stop toggle button, Esc keybinding to stop, Ctrl/Cmd+Enter to run. On budget exhaustion (default 50M), shows 'halted (budget)' with 'Increase budget 4x' button. Added js-sys dep for Date::now(). cargo check passes.