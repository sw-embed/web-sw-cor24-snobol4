Scaffold a Rust+Yew+Trunk WASM project. Create Cargo.toml (cdylib+rlib, edition 2024) with deps: yew 0.21 (csr), wasm-bindgen, web-sys (HtmlTextAreaElement, HtmlSelectElement, HtmlInputElement, KeyboardEvent, console), gloo, console_error_panic_hook, and path deps to ../sw-cor24-emulator (cor24-emulator, default-features=false) and ../sw-cor24-emulator/isa (cor24-isa, features=[serde]). Create Trunk.toml (dist=dist, port=9136), index.html, src/main.rs and src/lib.rs with a minimal Yew App that renders the page chrome from docs/design.md. Verify with 'cargo check'.