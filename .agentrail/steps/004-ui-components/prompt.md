Build the Yew App per docs/design.md: a single-column page with a header containing the title, a demo dropdown (entries from DEMOS in alphabetical order, default 'hello'), and a Run button; below it a source <textarea>, an input-data <textarea>, a status line, and an output <pre>. Selecting a demo replaces source (and loads input.dat for the 'input' demo). Wire Run to runner::run_snobol4 (synchronous for now). Ctrl/Cmd+Enter in the source area triggers Run.